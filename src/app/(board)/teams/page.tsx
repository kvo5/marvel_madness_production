import TeamsClientPage from './TeamsClientPage'; // We will create this next

const TeamsPage = () => {
  return (
    <div>
      {/* Render the client component that handles fetching and displaying teams */}
      <TeamsClientPage />
    </div>
  );
};

export default TeamsPage;